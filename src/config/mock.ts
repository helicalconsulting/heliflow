/**
 * Mock Configuration
 * 
 * Set USE_MOCK to `true` to use mock data (for development/demo).
 * Set USE_MOCK to `false` to connect to real backend APIs.
 * 
 * When migrating to production, simply change this single flag to `false`
 * and ensure your .env has the correct VITE_API_URL.
 */
export const USE_MOCK = true;
